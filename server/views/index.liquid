{% layout "server/layouts/base.liquid" %}

{% block content %}
<h1>{{ title }}</h1>
<ul class="grid">
  {% for league in leagues %}
  <li class="card">
    <a href="/competitions/{{ league.id }}">
      <h2>{{ league.name }}</h2>
    </a>
  </li>
  {% endfor %}
</ul>
{% endblock %}
